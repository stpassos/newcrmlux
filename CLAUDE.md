# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

NEWCRMLUX — CRM project (details to be added as the codebase grows).

## Commands

<!-- Add build, dev, test, and lint commands here once the project is initialized. -->
<!-- Example:
- `npm run dev` — start dev server
- `npm run build` — production build
- `npm test` — run tests
- `npm run lint` — lint code
-->

## Architecture

<!-- Add high-level architecture notes here once the project structure is established. -->
